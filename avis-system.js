// Système d'avis ORIF - Fonctions de base
console.log('📝 Système d\'avis ORIF chargé');

// Fonctions exportées pour la compatibilité
if (typeof window !== 'undefined') {
  window.avisSystemLoaded = true;
}